#include "position.h"

// Constructor implementation to initialize Position with given row and column
Position::Position(int row, int column)
{
    this->row = row; // Assigning row
    this->column = column; // Assigning column
}
