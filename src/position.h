#pragma once

class Position
{
public:
    Position(int row, int column); // Constructor to initialize Position with given row and column
    int row; // Row of the position
    int column; // Column of the position
};
